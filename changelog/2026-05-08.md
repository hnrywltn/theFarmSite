# Changes — May 8, 2026

## Project Setup
- Scaffolded full project: React 18 + Vite + Express + Tailwind CSS, npm workspaces monorepo
- Created CLAUDE.md with architecture documentation
- Color palette: farm-dark, farm-green, farm-cream, farm-gold, farm-earth
- Fonts: Cormorant Garamond (serif) + Jost (sans)

## Pages & Components
- **Home** — Nav, Hero, About, TheLand, Footer, Divider
- **Gallery** (`/gallery`) — masonry photo grid with lightbox, own page with Nav + Footer
- Nav logo links to `/`, Gallery link added

## Hero
- Animated collage background: three rows of farm photos scrolling in alternating directions
- Text block sits on solid `farm-dark` panel for contrast
- "Our Story" and "View Gallery →" CTAs

## Copy
- About: Charles building the farmhouse with boulders unearthed by tractor; Elaine and Charles buried at Wesley Chapel
- TheLand: The Cabin, The Bottoms & The Long Fields, Buck Creek
- Removed all trust/selling/future-generations language

## Google Drive Integration
- Server proxies Drive images through `/api/photos/:id` (avoids CORS/throttling)
- Photos listed newest-first (`createdTime desc`)
- Full Shared Drive support (`supportsAllDrives: true`) for upload quota
- Upload endpoint: `POST /api/photos/upload` — accepts multiple images via multer, writes to Drive folder

## Gallery — Upload
- **+ Add Photos** button top-right of gallery header
- Opens native file picker (multiple images, up to 20 at once)
- Uploads to Google Drive Shared Drive folder, gallery refreshes on completion

## Footer
- "Designed by Light Patterns" credit linking to lightpatternsonline.com

## Session — May 8, 2026 (evening)
- Established per-prompt changelog + commit convention for this session

## Auth / Login
- Added `AuthContext` with hardcoded password, `sessionStorage`-backed login state
- `LoginModal` component — password input, shows on "Sign In" click, error on wrong password
- Nav shows **Sign In** button when logged out; **Admin** link + **Sign Out** when logged in
- `/admin` route is protected — redirects to `/` if not logged in
- Gallery upload button and delete controls (grid hover + lightbox) hidden when not logged in

## Auth v2 — Per-user accounts + JWT
- Server: `users.json` seeded on first start with 7 family emails, all at `farmPassword2026`
- Server: `bcryptjs` hashing, `jsonwebtoken` (7d expiry) stored in `sessionStorage`
- Login modal now requires email + password; server returns JWT on success
- `AuthContext` rewired to server — `login()` POSTs to `/api/auth/login`, stores token + user
- Photo upload and delete endpoints now require `Authorization: Bearer <token>` on server
- Gallery passes token in upload/delete fetch headers
- **Admin — Change Password**: current + new + confirm fields, validated on server
- **Admin — Users**: list all users; add by email (gets default password); Suspend/Remove buttons scoped to users you added
- After adding a user, an invite modal pops up showing the site URL, email, and initial password with a Copy button — paste into a text or email
- `hnrywltn@gmail.com` is admin — can suspend/remove any user; others can only manage users they invited. No one can modify their own account.

## Photo attribution
- Users can set a display name in the admin panel (under Account Settings); falls back to email prefix if unset
- Upload renames files to `{name}__{original}` before sending to Drive
- Photo list parses and returns `uploader` field from filename
- Gallery grid: uploader name overlaid at the bottom of each photo
- Lightbox: uploader name shown below the image

## Activity Log (admin only)
- Server writes to `activity.json` (capped at 500 entries) on every action: sign-in, upload, delete photo, invite user, suspend/unsuspend/remove user, change password, update display name
- `GET /api/activity` — gated to admin email only
- Admin panel shows an Activity section at the bottom with actor name, human-readable action, and relative timestamp (hover for exact time)

## Postgres + Railway deployment
- Migrated from flat JSON files (`users.json`, `activity.json`) to Postgres
- `server/db.js` — pool, `mapUser()`, `init()` (creates tables + seeds 7 users on first run)
- `server/index.js` — all routes rewritten to use async DB queries
- Added `railway.toml` with build (`npm run build`) and start (`npm start`) commands
- Old JSON files added to `.gitignore`
- Added `nixpacks.toml` to force `--include=dev` on install so Vite is available at build time
- DNS configured on Namecheap: CNAME `@` + `www` → `lnvmzddn.up.railway.app`, TXT `_railway-verify`
- Guard all `/api/photos` fetch calls against non-array response (500 returned `{error}` object, `.filter()` crashed Hero + Photos + Gallery)
